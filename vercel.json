{
    "buildCommand": "npm install && ng build --configuration=production",
    "outputDirectory": "dist/cafeteria-web",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}